{
  "name": "zblog",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "rm -rf node_modules && rm -rf apps/**/node_modules && rm -rf packages/**/node_modules",
    "api:dev": "pnpm run --dir apps/api dev",
    "web:dev": "pnpm run --dir apps/web dev",
    "dev": "turbo run dev --filter=./apps/*"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "turbo": "^1.5.5",
    "typescript": "^4.8.4"
  }
}